input:
name - color
category - department
color, category - price


Output:
name+ = {name, color}
{name, category}+ = {name, category, color, department, price}
color+ = {color}

implementation plan :

1 map:
    key -> value
    
    name -> color
    category - > department
    <color, catagory> -> price

2 map:
  key+ -> value

  name+ -> name, color
        -> recursion on value
  category+ ->
